FROM maven:latest

# Установка рабочей директории
WORKDIR /usr/src/app

# Копирование исходного кода в контейнер
COPY . .

# Установка таймаута в 30 секунд перед запуском тестов
CMD ["sh", "-c", "sleep 80 && mvn test"]